# Required canonical public origin used by the watermark, metadata, docs and MCP URLs.
PUBLIC_SITE_URL=https://devshot.emadev.co

# Chrome Beta must be installed locally; Playwright selects the chrome-beta channel.
